
import javax.swing.*;

public class MyLabel extends JLabel{
    
    // JLabel label = new JLabel(); (this would be in the main or a constructor if not in its own class like this)
    //JLabel label = new JLabel("You can pass text directly into here too"); 

    MyLabel(){
    this.setText("setText in the this.setText function"); 
    // this.add(); 
    }
}
